---
# Meta information for cluster_drbd role

galaxy_info:
  role_name: cluster_drbd
  author: FlexiCluster Team
  description: Manages DRBD configuration for FlexiCluster
  company: Ericsson
  license: MIT-0
  min_ansible_version: "2.7"
  platforms:
    - name: Ubuntu
      versions:
        - focal
        - jammy
        - noble
  galaxy_tags:
    - cluster
    - drbd
    - replication
    - storage

dependencies:
  - role: cluster_base
  - role: cluster_storage
